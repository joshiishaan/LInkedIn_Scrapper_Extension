# Backend API base URL the extension talks to.
VITE_SERVER_BASE_URL=http://localhost:3000/api

# Debug logging. Leave unset/empty for production builds (all dlog/dwarn calls
# no-op). Set to "true" while developing to print the [conn-sync]/[conn-tracker]
# diagnostics to the console.
VITE_DEBUG=false
